import { Secret, SignOptions } from 'jsonwebtoken';

// Export the type of jsonwebtoken's Secret
export type JwtSecret = Secret;

// Export the type of jsonwebtoken's SignOptions
export type JwtSignOptions = SignOptions;
